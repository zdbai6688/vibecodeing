# Design Brief: UOS 速记 — UI 视觉重构

> 生成时间: 2026-08-06
> 版本: 1.0
> 关联 Spec: docs/product-spec.md
> 触发: 用户验收反馈「当前 UI 太丑、太工业风」，要求重新设计

## 1. 设计理念

### 1.1 风格关键词
温暖、圆润、现代、清爽、轻量（去「工业风」：去掉生硬分隔线/直角/扁平灰块）

### 1.2 设计原则
1. **暖色分层代替硬线分隔**：用 `alternateBase` 卡片 + 少量留白代替 1px 竖线分区，层次靠背景色差而非边框。[从 Spec 推导：DTK6 桌面应用]
2. **圆角体系**：主面板 12px、卡片 10px、按钮 8px、徽章胶囊 999px，统一视觉节奏。
3. **主题自适应**：全部颜色取 DPalette / QSS `palette(...)` 角色，亮/暗色自动切换，不硬编码大面积底色。[从 Spec 推导：支持深色/浅色主题]
4. **克制的强调色**：主操作/选中态用系统强调色（palette highlight），语义色（逾期红/提醒橙）仅用于状态，不作为大面积背景。
5. **排版留白**：页面边距 16-20px，卡片内边距 12-16px，行高 ≥1.5，字号层级 H1 16 / 正文 13 / 辅助 11。

## 2. 色彩系统

### 2.1 主色（跟随系统强调色，自适应主题）
| 用途 | 取值 | 说明 |
|------|------|------|
| Primary | `palette(highlight)` | 主按钮、选中态、链接 |
| Primary-soft | `palette(highlight)` @ 10% alpha | hover/选中背景 |

### 2.2 中性色（DPalette 角色映射）
| 用途 | 亮色取值 | 暗色取值 | 说明 |
|------|----------|----------|------|
| 页面背景 | `palette(base)` | 同左（角色） | 主内容底 |
| 卡片/侧栏 | `palette(alternateBase)` | 同左（角色） | 侧栏、周报卡片、分组头 |
| 悬浮层 | `palette(window)` | 同左（角色） | 快速录入窗、弹窗 |
| 边框 | `palette(mid)` | 同左（角色） | 输入框、次要描边 |
| 文本主 | `palette(windowText)` | 同左（角色） | 正文 |
| 文本次 | `palette(placeholderText)` | 同左（角色） | 辅助说明 |
| 禁用 | `palette(disabled)` | 同左（角色） | 禁用态 |

### 2.3 语义色
| 用途 | 色值 | 说明 |
|------|------|------|
| 逾期/删除 | `#E64545` | 仅用于状态徽章/文字 |
| 提醒/今日 | `#FAAD14` | 仅用于状态徽章/文字 |
| 本周/信息 | `#2178E5` | 仅用于状态徽章/文字 |
| 成功 | `#00A870` | 完成态 |

## 3. 字体系统
| 层级 | 字号 | 字重 | 用途 |
|------|------|------|------|
| H1（页面标题） | 16px | 700 | 周报周次、编辑器标题 |
| H2（区块标题） | 14px | 600 | 卡片标题、分组头 |
| Body | 13px | 400 | 正文、列表 |
| Caption | 11-12px | 400 | 辅助文本、时间 |
| Small | 10px | 500 | 徽章计数 |
字体族跟随系统（Noto Sans CJK SC / Source Han Sans）；等宽仅用于代码块。

## 4. 间距与布局
### 4.1 间距基准
| 名称 | 值 | 用途 |
|------|-----|------|
| xs | 4px | 图标与文字间隙 |
| sm | 8px | 同组元素间距 |
| md | 12-16px | 卡片内边距 |
| lg | 16-20px | 页面边距 |
| xl | 24px | 区块间分隔 |

### 4.2 圆角规范
| 名称 | 值 | 用途 |
|------|-----|------|
| sm | 6px | 小按钮/输入框 |
| md | 8-10px | 卡片/列表项 |
| lg | 12px | 主面板/弹窗 |
| full | 999px | 徽章/胶囊按钮 |

## 5. 组件风格
### 5.1 按钮
- Primary（新建/保存）：强调色底 + 白色字 + 8px 圆角 + hover 加深；侧栏「＋ 新建笔记」36px 高。
- Secondary（次要操作）：透明底 + `palette(mid)` 1px 描边 + 8px 圆角 + hover 强调色描边。
- Ghost（工具栏/图标）：透明底，hover 浅灰圆角。

### 5.2 输入框
圆角 8px、1px `palette(mid)` 描边、focus 强调色描边、内边距 8-12px。

### 5.3 卡片（待办行、笔记项、周报格）
`palette(light)` 底 + 8-10px 圆角 + hover `palette(midlight)`；选中态强调色底白字。已完成待办行整体降饱和（灰底 + 删除线 + ✓ 前缀）。

### 5.4 导航（侧栏）
`alternateBase` 底 + 圆角导航项；选中项强调色软底 + 强调色文字 + 600 字重；徽章胶囊。

### 5.5 日历/周报
7 列网格卡片：日期数字 34px 圆点，今天强调色实心；每格显示「N 项」数量徽章 + tooltip 明细；周末列弱化底色。

## 6. 图标与插画
- 类型: emoji 轻量图标（延续现状，成本最低）+ 系统主题图标（document-new 等）
- 尺寸: 16-28px 工具栏，32-48px 空状态

## 7. 动效规范
- 微交互：hover 背景过渡 150ms ease-out；弹窗出现无动画（DTK 默认）。
- 不引入重动画（办公工具，克制）。

## 8. 暗色/亮色模式
全量走 DPalette 角色，QSS 使用 `palette(...)` 关键字；主题切换由 DTK 统一驱动，各组件在 `themeTypeChanged` 时刷新样式。

## 9. 变更记录
| 版本 | 日期 | 变更内容 |
|------|------|----------|
| 1.0 | 2026-08-06 | 首版：去工业风，暖色分层 + 圆角体系 + 语义色约束；待办/周报/快速录入重点重构 |

## 10. 实施记录
| 文件 | 落实要点 |
|------|----------|
| `sidebarwidget.cpp` | 侧栏去竖线、导航圆角 8px、选中态强调色软底（10% alpha）、新建主按钮圆角 10px、徽章胶囊 |
| `mainwindow.cpp` | 三栏去 1px 分隔竖线，空状态改为圆角虚线卡片（16px） |
| `notelistwidget.cpp` | 笔记卡片圆角 10px + alternateBase、输入框/按钮圆角 6-8px |
| `todowidget.cpp` | 分组头改为「彩色圆点 + 文字」胶囊式，消除字体溢出；行内标签徽章限 2 个 + `+N` 折叠；「未安排」改名并加说明文案；卡片圆角 10px |
| `weeklyreportwidget.cpp` | 卡片圆角 12px、日期格圆角 10px、按钮圆角 8px |
| `quickentrydialog.cpp` | 紧凑窗 280×160→360×220、全屏窗 520×260→560×320，编辑区 60→84/120→160，控件圆角 8-12px |
| `noteeditorwidget.cpp` | 颜色取色器记住上次选色（不再每次重选）；预览改为文档副本上做行级 Markdown 转换，避免 `toHtml` 单行结构导致标题/列表失效 |
| `meetingwidget.cpp` | 会议列表卡片圆角 10px、主按钮/输入框圆角 8px |

## 11. 三轮实施记录（用户验收备注修复）
| 编号 | 问题 | 修复 |
|------|------|------|
| TC03① | 取色器闪退 | 上次选色栈局部变量 → 成员变量（lambda 悬垂引用 use-after-free） |
| TC03② | 标题栏图标过多 | 收敛为单个「⋯」菜单（新建/快速录入/设置/桌面模式并入） |
| TC03③ | 笔记列表拥挤 | 列表仅显示标题+时间+标签，正文只在右侧编辑器 |
| TC10① | 待办分组层级模糊 | 分组头统一「中性深色标题 + 彩色圆点 + 彩色计数徽章」 |
| TC10② | 待办右键功能少 | 新增「设置优先级」与「设置标签」（勾选/清除/新建） |
| TC13 | 快捷键无效 | 根因：Ctrl+Alt+Space 被 DDE 抢占（BadAccess）；改为候选键依次探测全局注册，自动回写 |
| TC14 | 双尺寸混乱 | 统一单窗口 440×240 单编辑器，输入区随内容自动增高 |
| TC19 | 第二次录音闪退 | `gst_bin_get_by_name` 子元素引用停止时未 unref → use-after-free，已统一释放 |

## 12. 四轮实施记录（2026-08-07 用户复测备注修复）
| 编号 | 问题 | 修复 |
|------|------|------|
| TC13 | 快捷键仍无效 | 根因两处：① Qt6/xcb 的 native 事件是 `xcb_generic_event_t*`，原代码按 Xlib `XEvent*` 解析（内存布局不同）→ 事件永远匹配不上；② GrabKey 必须用 **Qt 自己的 xcb 连接**（`QNativeInterface::QX11Application::connection()`），另开 Xlib Display 抓到的键事件不会进入 Qt 事件循环。已重写：Qt xcb 连接 + `xcb_grab_key` + 按 `xcb_key_press_event_t` 解析。真机验证 Ctrl+Alt+E 全局触发、输入保存落库成功 |
| TC14① | 快捷录入窗右上角 ✕ 显示不完整 | 关闭按钮改为独立样式 `compactClose`（28×28、圆角、hover 反馈），不再被 `compactBtn` 的 padding 裁剪 |
| TC14② | 「连续」含义不清 | 标签改「连续添加」并加 tooltip 说明「开启后保存不关闭窗口，可连续录入多条」 |
| TC14③ | 贴到桌面后找不到便签 | 贴到桌面成功后发送系统托盘通知，明确「便签显示在桌面右侧，点托盘图标可管理/返回」 |
| TC19 | 录音沙沙声较多 | 增益 1.5→1.0（原放大麦克风底噪）；audiodynamic 由 soft-knee 压缩改为 `mode=expander`（低于阈值安静段自动压低，抑制底噪）；已验证新管道可解析并产出 16kHz mono WAV |
| TC03② | 图片在编辑区显示为链接 | 新增 `insertImageMarkdown`：正文保留 `![](...)` Markdown（便于保存/迁移），同时将实际图片作为 QTextDocument 资源内联显示，编辑区直接看到图（等比缩放≤260px）；截图/粘贴/文件三路统一 |
| TC03③ | 标题栏「⋯」渲染成三角+点且错位 | 改用主题图标 `view-more-symbolic` 代替文本「⋯」 |
| TC10 | 顶部「待办」标题冗余、「未安排/其他」意义不明 | 分组场景隐藏顶部独立「待办」标题（各组自带分组头）；「未安排 / 其他」改名「未安排 / 无截止日期」，文案更明确 |
| TC22 | ASR「测试连接」显示占位提示 | 接入 `AsrServiceManager::testEngine`，与设置 Widget 一致：离线检查模型文件、在线校验凭据 |

## 13. 五轮实施记录（2026-08-07 用户复测备注修复）
| 编号 | 问题 | 修复 |
|------|------|------|
| TC03① | 颜色仍无法固定 | 颜色按钮改为**预设色板下拉菜单**（10 种常用色 + 自定义），点选即用并固定为当前色，按钮圆点实时显示当前色，不再每次打开取色器 |
| TC03② | 产品名应改「小U速记」 | 展示名（窗口标题/侧栏 logo/托盘 tooltip/desktop Name）改为「小U速记」；applicationName 保持 UOS速记 以兼容既有数据目录 |
| TC03③ | 侧边栏折叠不可用 | 根因：折叠后折叠按钮被隐藏，无法再展开。改为折叠按钮始终可见 |
| TC10① | 「未安排」含义不清 | 拆分为「无截止日期」「更晚到期」两组，语义直白 |
| TC10② | 已完成待办页无内容 | 根因：`TodoManager::getAllTodos()` 默认不含已完成项，completedOnly 页恒空。改用 `getCompletedTodos()` |
| TC13 | 贴桌面后双击/显示桌面便签消失 | 双击便签不再退出桌面模式（改为在主窗口聚焦该笔记并保持便签可见） |
| TC14 | Enter 保存也换行 | 根因：QTextEdit 自行消费 Enter。改用 `eventFilter` 在子控件层拦截 Enter→保存、Ctrl+Enter→换行 |
| TC19 | 沙沙声仍严重 | 参考系统语音记事本：换用 `pulsesrc`（硬件 AGC），去掉 audiodynamic 压缩器（增益/压缩组合不佳会在静音段放大底噪），带通改为 100–4000Hz 温和过滤；并修复 level `get-level` 信号检测避免刷屏告警 |
| TC20 | 会议搜索跳主页 | 搜索时列表为空显示「没有匹配的会议」提示，不再跳回会议主页 |

## 14. 六轮实施记录（2026-08-07 用户反馈修复）
| 编号 | 问题 | 修复 |
|------|------|------|
| 侧边栏① | 折叠"只折叠一部分" | 根因：`setFixedWidth` 后 QScrollArea 内容最小宽度撑开。改为直接 setFixedWidth + 限制滚动内容/滚动区最小宽度，折叠宽 48px、图标居中、文字/标签/分组全部隐藏 |
| 录音② | 沙沙声仍严重听不清 | 参考系统语音记事本：完全去掉 `audiochebband` 带通滤波器（滤波器 DSP 引入振铃伪影），链路简化为 `pulsesrc ! audioconvert ! audioresample ! audiorate ! wavenc`（16kHz mono）。已用真实麦克风验证产出正常 WAV |
| 最近删除③ | 内容无法删除 | 根因：`todo_tags`/`sticky_notes` 表外键引用 `notes_todos`，永久删除时未删关联记录导致外键约束阻止 DELETE。永久删除/批量删除/清空时先清关联表 |
| 会议④ | 会议主页右侧应有历史清单入口 | 列表为空时不再切到纯空状态页，统一显示列表页 + 「暂无历史会议」引导，保证始终有入口 |
| 贴桌面⑤ | 需关掉所有应用才能看到便签 | 根因：便签窗口设 `_NET_WM_STATE_BELOW`(置底) 被应用遮挡。改为 `WindowStaysOnTopHint` + `_NET_WM_STATE_STICKY`，便签悬浮在桌面与应用之上 |

## 15. 七轮实施记录（2026-08-07 用户对会议页布局反馈）
| 编号 | 问题 | 修复 |
|------|------|------|
| 会议页① | 会议主页没有历史清单入口，应保留原设计、最右侧增加清单、切分为三块 | 会议页改为「主内容区(原空状态/列表/详情) + 右侧历史会议清单栏(230px)」两栏布局，加主窗口侧栏即三块；历史清单始终显示全部历史会议，含搜索过滤与数量徽章，点击条目打开对应会议详情 |

## 16. 八轮实施记录（2026-08-07 用户对会议页/侧栏微调反馈）
| 编号 | 问题 | 修复 |
|------|------|------|
| 会议页① | 会议中间要恢复原来的模式（喇叭 + 开始会议） | 主区域进入会议页默认显示空状态引导页（🎤 喇叭 + 开始会议 + 新建会议）；搜索时切列表页；历史会议通过右侧清单访问 |
| 侧栏② | 折叠时还带着原来的数字角标，显得怪异 | 折叠时隐藏 navBadge 数字角标，仅保留图标 |
