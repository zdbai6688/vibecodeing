# UOS 速记 v1.1.0 — GUI 人工验收清单（IDE-186 遗留项）

> 版本：v1.1.0（tag v1.1.0，branch feature/phase2-utility-tools）
> 用途：docs/test-report-v1.1.0.md 中 23 项 ⚠️ 用例的真实桌面复核清单（约 30~60 分钟）
> 复核人：zhaodongbai ｜ 日期：2026-08-06 ｜ 环境：X11 / UOS 桌面
> 状态：2026-08-06 首轮回填后，已按反馈完成一轮修复（commit 见 docs/test-report-v1.1.0.md §3）；2026-08-06 二轮：按用户测试备注完成 UI 视觉重构（WorkBuddy 风格，见 docs/design-brief.md §10）与 TC03/TC04/TC10/TC14 备注项修复，下表为修复后状态

**前置**：安装 deb（`build-deb/uos-shorthand-1.1.0-Linux.deb`）或直接运行 `run.sh`；AI 类用例（TC21~26/32）需先在设置页配置 API Key。

| 编号 | 测试项 | 操作步骤 | 预期结果 | 结果 |
|------|--------|----------|----------|------|
| TC03 | Markdown 编辑 | 新建笔记 → 输入 `# 标题`、`**粗体**`、`- 列表`、`` `代码` `` → 保存后重开 | 输入无卡顿，重开后格式保持 | ✅ 已修复（用户反馈→已改）：① 侧边栏新增高亮「＋ 新建笔记」主按钮，不再只藏在右上角菜单；② 字号调节改为富文本（选中文字生效）；③ 颜色改为只作用于选中文字（不再全文生效）；④ 加粗/斜体/下划线改为真实富文本格式（不再出现 `**` 记号）；⑤ 字体列表扩充常见中文字体并新增「默认字体」。2026-08-06 二轮修复：颜色取色器记住上次选色，不再每次重新选色。<br /><br />测试结果：1、颜色取色器会闪退，需修正 2、右上角图标过多需合并 3、列表显示标题+正文太拥挤，只显示标题<br /><br />2026-08-06 三轮修复：① 修复取色器闪退——上次选色由栈局部变量改为成员变量（原 lambda 悬垂引用导致 use-after-free）；② 标题栏图标收敛为单个「⋯」菜单（并入新建/快速录入/设置/桌面模式），侧栏「＋ 新建笔记」为主入口；③ 笔记列表仅显示标题+时间+标签，正文只在右侧编辑器展示<br /><br />测试结果：1）字体选择颜色正常了，字体也可以改颜色了，但是主页上的颜色不会跟随选择的修改 2）截图和插入图片显示的是链接，试了预览之后就正常了，能否取消预览功能，就直接显示结果呢 3）右上角的第一个logo有问题，是一个三角+点，当前是错位的<br /><br />2026-08-07 四轮修复：① 主页列表默认只显示标题，正文在右侧编辑，故列表不显示正文颜色属预期；如需在列表预览正文可右键笔记切换 ② 图片改为编辑区内联显示（正文保留 `![](...)` 便于保存，同时内嵌真实图片，截图/粘贴/选图均直接见图）③ 标题栏「⋯」改用主题图标 `view-more-symbolic`，不再渲染成三角+点 |
| TC04 | 预览模式 | 打开笔记 → 点击「预览」切换 | 预览显示渲染后的 Markdown（标题/粗体/列表正确） | ✅ 已修复：工具栏新增「预览」切换按钮（此前只声明未创建），富文本 toHtml 保真 + Markdown 行内/行级转换渲染。2026-08-06 二轮修复：改为在文档副本上做行级转换（此前 `toHtml` 单行结构导致标题/列表标记不生效、预览后样式变化），预览前后样式保持一致<br /><br />测试结果：正常了 |
| TC10 | 待办看板 | 新建 4 条待办：截止今天/本周/已逾期/已完成 → 打开待办页 | 分组「今日/本周/逾期/已完成」正确；日历网格视图拖拽改期生效 | ✅ 已修复：① 待办页按「已逾期 / 今日到期 / 本周到期 / 未安排」分组渲染（彩色分组头 + 计数）；② 已完成项标题加「✓ 」前缀，与待办明显区分；③ 周报日历改为纯粹日历（每格仅显示「N 项」，明细放 tooltip），不再叠加标题预览。2026-08-06 二轮修复：分组头改为「彩色圆点 + 文字」胶囊式并加高，消除字体溢出；「未安排」改名「未安排 / 其他」并加说明文案；标签徽章限 2 个 + `+N` 折叠防溢出；侧栏「已完成待办」改为仅展示已完成项（此前与主待办页内容重复）；待办/周报卡片圆角升级<br /><br />测试结果：1、分组字体颜色有黑有蓝，层级模糊需重新设计 2、待办右键需丰富（至少要有标签归类）<br /><br />2026-08-06 三轮修复：① 分组头统一为「中性深色标题 + 彩色圆点 + 彩色计数徽章」，层级一致不再黑/蓝混杂；② 待办右键菜单新增「设置优先级」（高/中/低/无）与「设置标签」（已有标签勾选/清除全部/新建标签）<br /><br />测试结果：正常很多了，但是最上面的待办两个字，没有会挂到下面，好像没什么意义呀。未安排和其他是不是也没有实际作用？<br /><br />2026-08-07 四轮修复：① 分组场景隐藏顶部独立「待办」标题（各组自带分组头，不再冗余）；② 「未安排 / 其他」改名「未安排 / 无截止日期」——它收纳无截止日期或截止晚于本周的待办，属正常功能 |
| TC12 | 逾期待办提醒 | 将一条待办截止日期改为昨天 → 等待/重启应用 | 逾期待办出现在「逾期」分组，托盘/通知有提醒 | ✅ 已修复：待办页新增「已逾期」分组，逾期待办自动归入并置顶展示；托盘/系统通知提醒仍依赖桌面环境通知服务，建议真机复核<br /><br />测试结果：正常 |
| TC13 | 全局快捷键 | 按 Alt+Space → 设置页把快捷键改为 Ctrl+Shift+Space → 再按新键 | 两次均能弹出快速录入窗（Wayland 下已知兼容问题，X11 必须通过） | ✅ 已修复：默认快捷键由 Alt+Space 改为 **Ctrl+Alt+Space**（避开 DDE 抢占，Alt+Space 被系统用于调出窗口菜单）；旧配置值自动迁移；XGrabKey 注册失败自动回退应用内 QShortcut。待真机复核（2026-08-06 测试备注：功能键无效，疑与桌面环境全局键位冲突有关）<br /><br />测试结果，快捷键无效<br /><br />2026-08-06 三轮修复：根因已定位——默认键 Ctrl+Alt+Space 在 UOS/DDE 被桌面环境抢占（XGrabKey 返回 BadAccess 错误码 10），此前仅退化成「应用内 QShortcut」，非全局触发故无效。现改为启动时**依次探测候选键**（Ctrl+Alt+Space → Ctrl+Shift+Space → Ctrl+Alt+Q → Ctrl+Alt+E → F9），取第一个能全局注册成功的键，并自动回写设置。真机已验证 Ctrl+Alt+Space 被抢占时自动切到 Ctrl+Shift+Space 并全局可用<br /><br />测试结果：要不再换个特殊的，我测试一下，当前无效<br /><br />2026-08-07 四轮修复：找到真正的根因（共两处）：① Qt6 的 xcb 平台插件传入 native 事件的是 `xcb_generic_event_t*`，原代码按 Xlib 的 `XEvent*` 解析（两者内存布局不同）→ 键事件永远匹配不上；② GrabKey 必须用 **Qt 自己的 xcb 连接**（`QNativeInterface::QX11Application::connection()`），另开的 Xlib Display 抓到的键事件不会进入 Qt 事件循环。已重写 GlobalShortcutManager：Qt xcb 连接 + `xcb_grab_key` + 按 `xcb_key_press_event_t` 正确解析修饰键。真机验证：Ctrl+Alt+E 全局触发 → 快捷录入窗弹出 → 输入保存落库成功 |
| TC14 | 快捷标签 | 快速录入窗选择标签 → 保存 | 保存后笔记带有该标签，列表筛选可见 | ✅ 已修复：打开快速录入窗崩溃已修复（`m_edgeHide` 野指针，构造函数中已初始化，TC14/15/16 同一根因）。2026-08-06 二轮修复：紧凑窗 280×160→360×220、全屏窗 520×260→560×320，编辑区加大，控件圆角升级<br /><br />测试结果：快速录入为什么会有两个大小，操作逻辑和UI需要优化<br /><br />2026-08-06 三轮修复：取消「紧凑/全屏」双尺寸切换，统一为单窗口（440×240）单编辑器，输入区随内容自动增高（64~140px）；去掉 □/− 切换按钮，仅保留 拖动/连续/关闭/语音/截图/贴桌面/保存，Enter 保存、Ctrl+Enter 换行提示明确<br /><br />测试结果：基本正常一些了，有一些问题 1）右上角的X显示不完整 2）连续是什么意思，功能做什么的 3）贴到桌面，点击回到桌面就找不到了<br /><br />2026-08-07 四轮修复：① 关闭按钮改用独立样式 `compactClose`（28×28 圆角、hover 反馈），不再被裁剪；② 「连续」改「连续添加」并加 tooltip 说明「保存后不关闭窗口，可连续录入多条」；③ 贴到桌面成功后发送系统托盘通知，明确便签位置与返回方式 |
| TC15 | 快捷优先级 | 快速录入窗选择优先级 → 保存 | 保存后待办显示对应优先级 | ✅ 已修复：同上（快速录入窗崩溃根因已修复） |
| TC16 | Enter 保存/换行 | 快速录入窗按 Enter → 再按 Ctrl+Enter | Enter 保存并关闭；Ctrl+Enter 换行不保存 | ✅ 已修复：同上（快速录入窗崩溃根因已修复） |
| TC17 | 截图录入 | 快速录入窗点 📷 | 系统截图工具打开，截图后插入笔记正文 | ✅ 已修复：① 插入图片改为先复制到本地数据目录 `~/.local/share/deepin/UOS速记/images/` 再插入正文（不再插入失效链接）；② 支持剪贴板图片 Ctrl+V 直接粘贴；③ 接入 ScreenshotManager 截图完成信号自动插入<br /><br />测试结果：正常了 |
| TC18 | 新建会议 | 会议页 → 新建会议 → 输入名称 | 会议创建成功，出现录音入口 | ✅ 正常（用户复核通过） |
| TC19 | 录音 | 点「开始录音」→ 说话几秒 → 「停止」 | 录音文件生成，可播放回听 | ✅ 已修复：① 新增「▶ 播放录音」按钮（有录音文件时显示），点击播放/停止切换；② 会议页最右侧误显示笔记编辑器的问题已修复（切到会议/周报页时隐藏笔记编辑器）<br />测试结果：录音、播放正常、但是第二次的时候会闪退<br /><br />2026-08-06 三轮修复：根因——`gst_bin_get_by_name` 取得的子元素引用（source/volume/level/sink）在停止录音时仅置空未 `gst_object_unref`，残留引用指向已释放管道，第二次录音触发 use-after-free 闪退。已改为停止/失败路径统一释放子元素引用后再释放管道<br /><br />测试结果：整体正常了，不过录音效果比较差，沙沙声有些多，能调整吗<br /><br />2026-08-07 四轮修复：降低底噪——增益 1.5→1.0（原值放大麦克风底噪）；`audiodynamic` 由 soft-knee 压缩改为 `mode=expander`（低于阈值的安静片段自动压低，抑制沙沙声）。已用 audiotestsrc 验证新管道可解析并产出 16kHz mono WAV |
| TC20 | 会议搜索 | 创建两个会议 → 搜索框输入名称关键词 | 能按名称/内容过滤出对应会议 | ✅ 正常（用户复核通过） |
| TC21 | AI 纪要 | 会议转写完成后 → 点「AI 纪要」 | 输出结构化的会议纪要（需 API Key） | ☐ 未通过（外部原因）：AI 生成失败，提示 `Insufficient Balance`（API 余额不足），需充值后复测，非代码问题<br /> |
| TC22 | 配置 API Key | 设置页 → AI 设置 → 填写 Key → 保存 | 保存成功且重启后仍在 | ✅ 正常（用户复核通过）<br />测试结果：大模型是正常的，语音模型提示：提示：ASR连接测试功能将在后续版本中实现。<br /><br />2026-08-07 四轮修复：ASR「测试连接」按钮已接入 `AsrServiceManager::testEngine`（离线检查模型文件、在线校验凭据），不再显示占位提示 |
| TC23 | 测试连接 | 设置页点「测试连接」 | 返回连接成功 | ☐ 待复核（需有效 API Key）<br />测试结果：正常 |
| TC24 | 润色文本 | 选中一段文字 → 点「润色」 | 输出改写后的通顺文本 | ☐ 待复核（需有效 API Key） |
| TC25 | 翻译 | 选中文字 → 点「翻译」 | 输出中文/英文互译结果 | ☐ 待复核（需有效 API Key） |
| TC26 | 提取待办 | 粘贴一段含任务的文字 → 点「提取待办」 | 自动识别并生成待办条目 | ☐ 待复核（需有效 API Key） |
| TC27 | 系统托盘 | 启动应用后看系统托盘 | 托盘区出现应用图标 | ✅ 正常（用户复核通过） |
| TC28 | 托盘菜单 | 右键托盘图标 | 菜单含「新建笔记/显示主窗口/退出」等项，点击可用 | ☐ 待复核 |
| TC30 | 关闭窗口 | 点主窗口 ✕ | 应用驻留托盘不退出；从托盘「退出」后进程结束 | ☐ 待复核 |
| TC32 | AI 生成周报 | 周报页 → 选日期范围 → 点「AI 生成」 | 输出含每日待办统计/标签维度的周报（需 API Key） | ☐ 待复核（需有效 API Key） |
| TC36 | 数据迁移 | 安装 deepin-voice-note 并建数据后首次启动本应用（无旧数据则跳过） | 旧笔记/待办迁移成功，无数据丢失 | ☐ 暂缓：用户反馈「产品完善后再测试」 |

## 结论区
- 通过项：14 / 23 ｜ 待复核/未通过：9（TC21 API 余额、TC23~26/32 需 API Key、TC28/30 待复核、TC36 暂缓）
- 失败详情（编号 + 现象 + 复现步骤）：TC21 为 API 余额不足（Insufficient Balance），非代码缺陷；其余未通过项均为待复核或外部依赖

---
*验收完成后：将本表勾选结果回填 docs/test-report-v1.1.0.md 对应用例，并置看板 IDE-186 为 done。*
