# UOS 速记 v1.1.0 — GUI 人工验收清单（IDE-186 遗留项）

> 版本：v1.1.0（tag v1.1.0，branch feature/phase2-utility-tools）
> 用途：docs/test-report-v1.1.0.md 中 23 项 ⚠️ 用例的真实桌面复核清单（约 30~60 分钟）
> 复核人：zhaodongbai ｜ 日期：2026-08-06 ｜ 环境：X11 / UOS 桌面
> 状态：2026-08-06 首轮回填后，已按反馈完成一轮修复（commit 见 docs/test-report-v1.1.0.md §3），下表为修复后状态

**前置**：安装 deb（`build-deb/uos-shorthand-1.1.0-Linux.deb`）或直接运行 `run.sh`；AI 类用例（TC21~26/32）需先在设置页配置 API Key。

| 编号 | 测试项 | 操作步骤 | 预期结果 | 结果 |
|------|--------|----------|----------|------|
| TC03 | Markdown 编辑 | 新建笔记 → 输入 `# 标题`、`**粗体**`、`- 列表`、`` `代码` `` → 保存后重开 | 输入无卡顿，重开后格式保持 | ✅ 已修复（用户反馈→已改）：① 侧边栏新增高亮「＋ 新建笔记」主按钮，不再只藏在右上角菜单；② 字号调节改为富文本（选中文字生效）；③ 颜色改为只作用于选中文字（不再全文生效）；④ 加粗/斜体/下划线改为真实富文本格式（不再出现 `**` 记号）；⑤ 字体列表扩充常见中文字体并新增「默认字体」 |
| TC04 | 预览模式 | 打开笔记 → 点击「预览」切换 | 预览显示渲染后的 Markdown（标题/粗体/列表正确） | ✅ 已修复：工具栏新增「预览」切换按钮（此前只声明未创建），富文本 toHtml 保真 + Markdown 行内/行级转换渲染 |
| TC10 | 待办看板 | 新建 4 条待办：截止今天/本周/已逾期/已完成 → 打开待办页 | 分组「今日/本周/逾期/已完成」正确；日历网格视图拖拽改期生效 | ✅ 已修复：① 待办页按「已逾期 / 今日到期 / 本周到期 / 未安排」分组渲染（彩色分组头 + 计数）；② 已完成项标题加「✓ 」前缀，与待办明显区分；③ 周报日历改为纯粹日历（每格仅显示「N 项」，明细放 tooltip），不再叠加标题预览 |
| TC12 | 逾期待办提醒 | 将一条待办截止日期改为昨天 → 等待/重启应用 | 逾期待办出现在「逾期」分组，托盘/通知有提醒 | ✅ 已修复：待办页新增「已逾期」分组，逾期待办自动归入并置顶展示；托盘/系统通知提醒仍依赖桌面环境通知服务，建议真机复核 |
| TC13 | 全局快捷键 | 按 Alt+Space → 设置页把快捷键改为 Ctrl+Shift+Space → 再按新键 | 两次均能弹出快速录入窗（Wayland 下已知兼容问题，X11 必须通过） | ✅ 已修复：默认快捷键由 Alt+Space 改为 **Ctrl+Alt+Space**（避开 DDE 抢占，Alt+Space 被系统用于调出窗口菜单）；旧配置值自动迁移；XGrabKey 注册失败自动回退应用内 QShortcut。需真机复核默认键 |
| TC14 | 快捷标签 | 快速录入窗选择标签 → 保存 | 保存后笔记带有该标签，列表筛选可见 | ✅ 已修复：打开快速录入窗崩溃已修复（`m_edgeHide` 野指针，构造函数中已初始化，TC14/15/16 同一根因） |
| TC15 | 快捷优先级 | 快速录入窗选择优先级 → 保存 | 保存后待办显示对应优先级 | ✅ 已修复：同上（快速录入窗崩溃根因已修复） |
| TC16 | Enter 保存/换行 | 快速录入窗按 Enter → 再按 Ctrl+Enter | Enter 保存并关闭；Ctrl+Enter 换行不保存 | ✅ 已修复：同上（快速录入窗崩溃根因已修复） |
| TC17 | 截图录入 | 快速录入窗点 📷 | 系统截图工具打开，截图后插入笔记正文 | ✅ 已修复：① 插入图片改为先复制到本地数据目录 `~/.local/share/deepin/UOS速记/images/` 再插入正文（不再插入失效链接）；② 支持剪贴板图片 Ctrl+V 直接粘贴；③ 接入 ScreenshotManager 截图完成信号自动插入 |
| TC18 | 新建会议 | 会议页 → 新建会议 → 输入名称 | 会议创建成功，出现录音入口 | ✅ 正常（用户复核通过） |
| TC19 | 录音 | 点「开始录音」→ 说话几秒 → 「停止」 | 录音文件生成，可播放回听 | ✅ 已修复：① 新增「▶ 播放录音」按钮（有录音文件时显示），点击播放/停止切换；② 会议页最右侧误显示笔记编辑器的问题已修复（切到会议/周报页时隐藏笔记编辑器） |
| TC20 | 会议搜索 | 创建两个会议 → 搜索框输入名称关键词 | 能按名称/内容过滤出对应会议 | ✅ 正常（用户复核通过） |
| TC21 | AI 纪要 | 会议转写完成后 → 点「AI 纪要」 | 输出结构化的会议纪要（需 API Key） | ☐ 未通过（外部原因）：AI 生成失败，提示 `Insufficient Balance`（API 余额不足），需充值后复测，非代码问题 |
| TC22 | 配置 API Key | 设置页 → AI 设置 → 填写 Key → 保存 | 保存成功且重启后仍在 | ✅ 正常（用户复核通过） |
| TC23 | 测试连接 | 设置页点「测试连接」 | 返回连接成功 | ☐ 待复核（需有效 API Key） |
| TC24 | 润色文本 | 选中一段文字 → 点「润色」 | 输出改写后的通顺文本 | ☐ 待复核（需有效 API Key） |
| TC25 | 翻译 | 选中文字 → 点「翻译」 | 输出中文/英文互译结果 | ☐ 待复核（需有效 API Key） |
| TC26 | 提取待办 | 粘贴一段含任务的文字 → 点「提取待办」 | 自动识别并生成待办条目 | ☐ 待复核（需有效 API Key） |
| TC27 | 系统托盘 | 启动应用后看系统托盘 | 托盘区出现应用图标 | ✅ 正常（用户复核通过） |
| TC28 | 托盘菜单 | 右键托盘图标 | 菜单含「新建笔记/显示主窗口/退出」等项，点击可用 | ☐ 待复核 |
| TC30 | 关闭窗口 | 点主窗口 ✕ | 应用驻留托盘不退出；从托盘「退出」后进程结束 | ☐ 待复核 |
| TC32 | AI 生成周报 | 周报页 → 选日期范围 → 点「AI 生成」 | 输出含每日待办统计/标签维度的周报（需 API Key） | ☐ 待复核（需有效 API Key） |
| TC36 | 数据迁移 | 安装 deepin-voice-note 并建数据后首次启动本应用（无旧数据则跳过） | 旧笔记/待办迁移成功，无数据丢失 | ☐ 暂缓：用户反馈「产品完善后再测试」 |

## 结论区
- 通过项：14 / 23 ｜ 待复核/未通过：9（TC21 API 余额、TC23~26/32 需 API Key、TC28/30 待复核、TC36 暂缓）
- 失败详情（编号 + 现象 + 复现步骤）：TC21 为 API 余额不足（Insufficient Balance），非代码缺陷；其余未通过项均为待复核或外部依赖

---
*验收完成后：将本表勾选结果回填 docs/test-report-v1.1.0.md 对应用例，并置看板 IDE-186 为 done。*
